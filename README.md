
ag-Grid
==============

Example of using ag-Grid with Angular2 and TypeScript

Building
==============

To build:
- `npm install`
- `npm install tsd -g`
- `tsd install es6-shim`
- `npm run tsc`

To Run:
- `npm run lite`
